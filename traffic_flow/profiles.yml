traffic_flow:
    target: dev
    outputs:
        dev:
            type: postgres
            host: localhost
            user: airflow
            pass: airflow
            port: 5432
            dbname: test
            schema: traffic_flow_dbt
            threads: 4
